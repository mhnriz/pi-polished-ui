# Node
node_modules/
*.tgz

# Build / generated
dist/
build/
*.log

# Validation output (generated by scripts/validate.sh — never commit)
scripts/out/
*.val.log

# Backups created by scripts/install.sh
*.bak-*
**/*.bak-*

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
*~

# Secrets / local config (defensive — these never belong in the repo)
.env
.env.*
*.key
*.pem

# pi internals (should never be created inside the repo, but guard anyway)
auth.json
models-store.json
settings.json
sessions/
